[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "spikestack-lite"
version = "1.1.0"
description = "5-in-1 spiking transformer: Spire encoder + AstroHebbian spiking attention + SpikeSkip CUDA sparse engine + GSMC memory + Exact-SNN latency head"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = [
    "torch>=2.0",
    "torchvision>=0.15",
]

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["."]
include = ["spikestack_lite*"]

[tool.setuptools.package-data]
"spikestack_lite.sparse" = ["src/*.cu"]